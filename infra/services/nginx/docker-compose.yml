services:
  nginx:
    container_name: nginx
    image: nginx:alpine
    networks:
      - backend
    ports:
      - "80:80"
    restart: unless-stopped
    volumes:
      - ../../nginx/nginx.conf:/etc/nginx/nginx.conf:ro

networks:
  backend:
    external: true
    name: infra_backend
